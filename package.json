{
  "name": "bar4bar",
  "version": "0.1.0",
  "description": "Word-by-word lyrics that follow every bar, in sync.",
  "type": "module",
  "main": "electron/main.cjs",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "node server.mjs",
    "start": "env -u ELECTRON_RUN_AS_NODE electron .",
    "test": "SEPARATE_MODEL_URL= SEPARATE_MODEL_PATH= node --test --test-force-exit test/*.test.js"
  },
  "devDependencies": {
    "electron": "^31.0.0"
  },
  "dependencies": {
    "@huggingface/transformers": "^4.2.0"
  }
}
